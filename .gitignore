# Variáveis de ambiente na raiz do monorepo (nunca versionar segredos)
.env
.env.*
!.env.example

# Dependências na raiz, se existirem
node_modules/

# Cópia de `.next` na raiz só no deploy Vercel (scripts/vercel-monorepo-sync.mjs)
/.next/
